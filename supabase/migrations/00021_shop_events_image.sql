-- イベント特設リールページ用: イベントに縦長画像を持たせる。
-- 既存行に画像がないケースがあるため列自体はNULL許容のままとし、
-- 「リールに乗せる画像が必須」という制約はアプリ側(登録フォーム)で担保する
-- (画像のないイベントはポータルのイベントリールには表示しないだけで、
-- 店舗ページのイベント告知一覧には引き続き表示できるようにするため)。
alter table public.shop_events
  add column image_url text;
